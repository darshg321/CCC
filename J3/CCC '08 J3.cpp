#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main (int argc, char *argv[]) {
  
  vector<string> grid = {"ABCDEF", "GHIJKL", "MNOPQR", "STUVWX", "YZ -.*"};

  string msg;
  getline(cin, msg);
  vector<int, int> pos = {0, 0}
  for(auto i : msg) {
    
  }
  


  return 0;
}
